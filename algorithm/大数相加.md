
```javascript

String.prototype.paddingLeft = function(length:number,paddingValue:string){
  return String(paddingValue + this).slice(-length);
}
function addBigNumbers(num1, num2) {
  // 将两个大数转换为字符串，并获取其长度
  
}

const num1 = '12345678901234567890';
const num2 = '98765432109876543210';
const sum = addBigNumbers(num1, num2);
console.log(sum); // 输出：111111111011111111100>)
```